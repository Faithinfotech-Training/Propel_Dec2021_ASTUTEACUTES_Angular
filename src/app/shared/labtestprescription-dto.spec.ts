import { LabtestprescriptionDTO } from './labtestprescription-dto';

describe('LabtestprescriptionDTO', () => {
  it('should create an instance', () => {
    expect(new LabtestprescriptionDTO()).toBeTruthy();
  });
});
